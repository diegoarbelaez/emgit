ALTER TABLE `contratista` ADD `departamento` VARCHAR(255) NOT NULL AFTER `alergias`;
ALTER TABLE `contratista` ADD `municipio` VARCHAR(255) NOT NULL AFTER `departamento`
ALTER TABLE `contratista`  ADD `nombre_corregimiento` VARCHAR(255) NOT NULL  AFTER `municipio`
ALTER TABLE `contratista` ADD `descripcion_ubicacion` VARCHAR(255) NOT NULL AFTER `nombre_corregimiento`
ALTER TABLE `contratista` ADD `nombres_conyugue` VARCHAR(255) NOT NULL AFTER `descripcion_ubicacion`, ADD `apellidos_conyugue` VARCHAR(255) NOT NULL AFTER `nombres_conyugue`, ADD `cedula_conyugue` VARCHAR(255) NOT NULL AFTER `apellidos_conyugue`, ADD `fecha_nacimiento_conyugue` DATE NULL DEFAULT NULL AFTER `cedula_conyugue`, ADD `nombres_padre` VARCHAR(255) NOT NULL AFTER `fecha_nacimiento_conyugue`, ADD `apellidos_padre` VARCHAR(255) NOT NULL AFTER `nombres_padre`, ADD `cedula_padre` VARCHAR(255) NOT NULL AFTER `apellidos_padre`, ADD `fecha_nacimiento_padre` DATE NULL DEFAULT NULL AFTER `cedula_padre`, ADD `nombres_madre` VARCHAR(255) NOT NULL AFTER `fecha_nacimiento_padre`, ADD `apellidos_madre` VARCHAR(255) NOT NULL AFTER `nombres_madre`, ADD `cedula_madre` VARCHAR(255) NOT NULL AFTER `apellidos_madre`, ADD `fecha_nacimiento_madre` DATE NULL DEFAULT NULL AFTER `cedula_madre`;