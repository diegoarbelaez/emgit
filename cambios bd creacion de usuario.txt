Tenga en cuenta que debes copiar el archivo de Bootstrap en la carpeta, para que funcione como local
Tenga en cuenta a ver si carga el logo de MegaInforme en la sección de contratistas, después de que todo quede up and running


ALTER TABLE `contratista` ADD `departamento` VARCHAR(255) NOT NULL AFTER `tipo_vivienda`, ADD `municipio` VARCHAR(255) NOT NULL AFTER `departamento`, ADD `nombre_corregimiento` VARCHAR(255) NOT NULL AFTER `municipio`, ADD `descripcion_ubicacion` VARCHAR(255) NOT NULL AFTER `nombre_corregimiento`;
ALTER TABLE `contratista` ADD `nombres_conyugue` VARCHAR(255) NOT NULL AFTER `alergias`, ADD `apellidos_conyugue` VARCHAR(255) NOT NULL AFTER `nombres_conyugue`, ADD `cedula_conyugue` VARCHAR(255) NOT NULL AFTER `apellidos_conyugue`, ADD `fecha_nacimiento_conyugue` VARCHAR(255) NOT NULL AFTER `cedula_conyugue`, ADD `nombres_padre` VARCHAR(255) NOT NULL AFTER `fecha_nacimiento_conyugue`, ADD `apellidos_padre` VARCHAR(255) NOT NULL AFTER `nombres_padre`, ADD `cedula_padre` VARCHAR(255) NOT NULL AFTER `apellidos_padre`, ADD `fecha_nacimiento_padre` VARCHAR(255) NOT NULL AFTER `cedula_padre`, ADD `nombres_madre` VARCHAR(255) NOT NULL AFTER `fecha_nacimiento_padre`, ADD `apellidos_madre` VARCHAR(255) NOT NULL AFTER `nombres_madre`, ADD `cedula_madre` VARCHAR(255) NOT NULL AFTER `apellidos_madre`, ADD `fecha_nacimiento_madre` VARCHAR(255) NOT NULL AFTER `cedula_madre`;
ALTER TABLE `contratista` CHANGE `fecha_nacimiento_conyugue` `fecha_nacimiento_conyugue` DATE NOT NULL;
ALTER TABLE `contratista` CHANGE `fecha_nacimiento_padre` `fecha_nacimiento_padre` DATE NOT NULL;
ALTER TABLE `contratista` CHANGE `fecha_nacimiento_madre` `fecha_nacimiento_madre` DATE NOT NULL;
ALTER TABLE `contratista` ADD `nombre_pregrado` VARCHAR(255) NOT NULL AFTER `nivel_educativo`, ADD `proceso_pregrado` VARCHAR(255) NOT NULL AFTER `nombre_pregrado`, ADD `nombre_posgrado` VARCHAR(255) NOT NULL AFTER `proceso_pregrado`, ADD `proceso_posgrado` VARCHAR(255) NOT NULL AFTER `nombre_posgrado`;
ALTER TABLE `contratista` ADD `eps` VARCHAR(255) NOT NULL AFTER `fecha_nacimiento_madre`, ADD `arl` VARCHAR(255) NOT NULL AFTER `eps`, ADD `caja` VARCHAR(255) NOT NULL AFTER `arl`, ADD `pensiones` VARCHAR(255) NOT NULL AFTER `caja`, ADD `nombre_emergencia` VARCHAR(255) NOT NULL AFTER `pensiones`, ADD `numero_emergencia` VARCHAR(255) NOT NULL AFTER `nombre_emergencia`, ADD `actividades` TEXT NOT NULL AFTER `numero_emergencia`, ADD `variables_culturales` TEXT NOT NULL AFTER `actividades`;
ALTER TABLE `contratista` ADD `rasgos` VARCHAR(255) NOT NULL AFTER `variables_culturales`, ADD `grupos` VARCHAR(255) NOT NULL AFTER `rasgos`, ADD `discapacidad` VARCHAR(255) NOT NULL AFTER `grupos`;
INSERT INTO `contrato` (`id_contrato`, `numero`, `nombre`, `fecha_inicio`, `fecha_fin`, `objeto`, `supervisor1`, `supervisor2`, `correo_supervisor1`, `correo_supervisor2`, `valor`, `numero_cuotas`, `fk_id_dependencia`, `indicador`, `indicador2`, `indicador3`, `activo`) VALUES ('9999', 'CONTRATO DEFAULT - QUE SE ASOCIA AL INICIO', 'CONTRATO DEFAULT - QUE SE ASOCIA AL INICIO', '2021-01-01', '2021-01-01', 'CONTRATO DEFAULT - QUE SE ASOCIA AL INICIO', 'CONTRATO DEFAULT - QUE SE ASOCIA AL INICIO', 'CONTRATO DEFAULT - QUE SE ASOCIA AL INICIO', 'CONTRATO DEFAULT - QUE SE ASOCIA AL INICIO', 'CONTRATO DEFAULT - QUE SE ASOCIA AL INICIO', '0', '1', '17', 'CONTRATO DEFAULT - QUE SE ASOCIA AL INICIO', 'CONTRATO DEFAULT - QUE SE ASOCIA AL INICIO', 'CONTRATO DEFAULT - QUE SE ASOCIA AL INICIO', '1')
ALTER TABLE `contratista` ADD `estrato` VARCHAR(10) NOT NULL AFTER `barrio`;